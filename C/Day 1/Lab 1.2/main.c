#include <stdio.h>
#include <stdlib.h>

int main() // C Program to print ASCII number of a given char
{
    char c;
    printf("Enter a character\n");
    scanf("%c", &c);
    printf("character = %c in ASXII = %d\n", c, c);
    return 0;
}
