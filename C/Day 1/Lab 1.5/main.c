#include <stdio.h>
#include <stdlib.h>

int main() // C Program to print Hexa of a number
{
    int x;
    printf("Enter a Number\n");
    scanf("%d", &x);
    printf("The number %d in hexa = %X\n", x, x);
    return 0;
}
